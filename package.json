{
  "name": "gsd-core",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "GSD workflow skills, contract validator, and OMP session extension.",
  "types": "extensions/gsd-context.d.ts",
  "engines": {
    "bun": ">=1.3.14"
  },
  "os": [
    "linux"
  ],
  "scripts": {
    "test": "bun test --timeout=30000 test/*.test.js",
    "lint": "bunx --yes @biomejs/biome@2.5.8 lint .",
    "format": "bunx --yes @biomejs/biome@2.5.8 format --write",
    "lint:shell": "shellcheck install.sh"
  }
}
